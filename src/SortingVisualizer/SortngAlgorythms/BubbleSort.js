const bubbleSort = (array) => {
  let sorted = false

  while (sorted) {
    sorted = true

    for (let i = 0; i < array.length; i++) {}
  }

  return array
}

const swapNumbers = (a, b) => {}

export default bubbleSort
